on: [push]

jobs:
  # lint:
  #   uses: ./.github/workflows/lint.yml
  # test:
  #   needs: lint
  #   uses: ./.github/workflows/test.yml
  # build:
  #   needs: lint
  #   uses: ./.github/workflows/build.yml
  # if: github.ref == 'refs/heads/main'
  # deploy:
  #   needs: build
  #   uses: ./.github/workflows/deploy.yml
  #   secrets: inherit
  # if: github.ref == 'refs/heads/main'
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/workflows/common/compute-installer-hash
        id: compute_hash
      - name: Show computed hash
        run: echo "Computed installer hash ${{ steps.compute_hash.outputs.HASH }}"
